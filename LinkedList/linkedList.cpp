#include<iostream>
using namespace std;

class Node{
    public:
    int data;
    Node* next;
    Node(){

    }
    Node(int data){
        this->data = data;
        this->next = NULL;
    }
};
// Insertion at end
Node * Insert(){
    int val;
    cin>>val;
    Node * head = NULL;
    Node * tail = NULL;

    while (val>0)
    {
       if (head==NULL)
       {
            
       }
       
    }
    
}
int main(){
    Node *head = new Node();
    
    return 0;
}